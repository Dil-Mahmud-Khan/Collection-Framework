package com.exapi.collectionframeworks.theory;

public class CollectionTheory {
    // root interface of all collection interface
    // some collection methods are
    /*
    int size()                return number of elements
    boolean isEmpty()         return true or false if element is there or not
    boolean add(E)            Je object pathano hoy paramenter e, oita add kore
    boolean remove(E)         remove kore object
    boolean contains(Object O) object collection e thakle returns true;
    Iterator<E> iterator()    return iterator object, ja diye collection er sob element traverse kora jay
    Object[] toArray()         Collection er sob object array hisebe dorkar hole

    boolean containsAll(Collection<?> c)
    boolean addAll(Collection<?> c)
    boolean removeAll(Collection <?> c)         Eigula collection hisebe parameter ney ar check kore

     */
}
