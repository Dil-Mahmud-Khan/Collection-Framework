package com.exapi.collectionframeworks.theory;

public class QueueTheory {
    public static void main(String[] args) {
        /*
        FIFO
        Maintain order

        E element()                 return first element from queue// if no element then throw NoSucElementException
        E peek()                return first element// if no then return null
        boolean add(E e)        add elements and return true// if can't then return IllegalStateException
        boolean offer(E e)      add element to queue and return ture// if can't return false
        E remove()              remove the first top element// if no element found ,throw NoSuchElementException
        E poll()                return head element and remove// if no found return null


        Two implementation of this are:
        1.LinkedList
        2.PriorityQueue

         */
    }
}
